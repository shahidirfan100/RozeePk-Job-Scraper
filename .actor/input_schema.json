{
  "title": "RozeePk Jobs Scraper",
  "description": "Scrape job listings from Rozee.pk with flexible search options.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrl": {
      "title": "Start URL (Optional)",
      "type": "string",
      "description": "Direct RozeePk search URL. Overrides keyword/location if provided.",
      "editor": "textfield"
    },
    "keyword": {
      "title": "Keyword",
      "type": "string",
      "description": "Job search keyword (e.g., 'Software Engineer', 'Marketing Manager').",
      "editor": "textfield",
      "prefill": "software engineer"
    },
    "location": {
      "title": "Location",
      "type": "string",
      "description": "Filter by city or region (e.g., 'Lahore', 'Karachi', 'Islamabad').",
      "editor": "textfield"
    },
    "results_wanted": {
      "title": "Maximum Jobs",
      "type": "integer",
      "description": "Maximum number of jobs to scrape.",
      "minimum": 1,
      "maximum": 1000,
      "default": 100,
      "editor": "number"
    },
    "max_pages": {
      "title": "Maximum Pages",
      "type": "integer",
      "description": "Maximum search result pages to visit.",
      "minimum": 1,
      "maximum": 100,
      "default": 20,
      "editor": "number"
    },
    "proxyConfiguration": {
      "title": "Proxy Configuration",
      "type": "object",
      "description": "Proxy settings. Residential proxies recommended for best results.",
      "editor": "proxy",
      "default": {
        "useApifyProxy": true,
        "apifyProxyGroups": [
          "RESIDENTIAL"
        ]
      }
    }
  },
  "required": []
}